name: development for base images
on:
    pull_request:
        types:
            - created
            - opened
            - edited
            - synchronize
            - reopened
    workflow_call:

jobs:
    build-base-image:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write
        steps:
            -   uses: actions/checkout@v3

            -   name: Login to GitHub Container Registry
                uses: docker/login-action@v1
                with:
                    registry: ghcr.io
                    username: ${{ github.actor }}
                    password: ${{ secrets.GITHUB_TOKEN }}

            -   name: Build and push base image for this branch
                run: |
                    echo ${GITHUB_HEAD_REF}
#                    docker build . -f php-fpm/build/Dockerfile --build-arg BASE_IMAGE_PHP="php:7.4.30-fpm-buster" --tag ghcr.io/cbrooney/base-php-fpm-prod:${GITHUB_HEAD_REF}
#                    docker push ghcr.io/cbrooney/base-php-fpm-prod:${GITHUB_HEAD_REF}
#                    docker build . -f php-fpm/build/Dockerfile --build-arg BASE_IMAGE_PHP="php:7.4.30-fpm-buster" --tag ghcr.io/cbrooney/base-php-fpm-dev:${GITHUB_HEAD_REF}
#                    docker push ghcr.io/cbrooney/base-php-fpm-dev:${GITHUB_HEAD_REF}
